version: '3'

services:
  backend:
    build: 'backend/'
    depends_on:
      - db
    environment:
      SQLALCHEMY_DATABASE_URI: 'postgresql://ecom_admin:password@db:5432/ecom'
      VERIFICATION_LINK: 'http://3.109.161.249:5000/user/verify'
      EMAIL_ID: 'pranav.nair@sculptsoft.com'
      PASSWORD: 'adfly143'
      TOKEN_SECRET: 'qwertyuioplkmjnha5526735gbsgsg'
    ports:
      - '5000:5000'
    restart: always
    volumes:
      - ./:/usr/src/app
      - ./backend/migrations:/var/lib/postgresql/data
  db:
    image: postgres:latest
    networks:
      - default
    ports:
      - 5405:5432
    restart: always
    environment:
      POSTGRES_USER: ecom_admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ecom